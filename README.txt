> A folder to store everything that can be used in a longer term so that I only worry about one folder when move to a new machine. The files include datasets, installation files, cuda runfiles, library files, features, project-specific cache fiels.




